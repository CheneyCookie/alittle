package xml.day03.dom;

public class DomParse {
	public void read(){
		//1.生成document对象
		
		//2.获取根元素getDocumentElement
		
		/**
		 * getChildNodes() 获取所有的子节点
		 * rootChildNodes.getLength() 获取节点列表的长度
		 * rootChildNodes.item(i) 获取单个节点
		 * node.getNodeName() 获取节点名字
		 * node.getTextContent() 获取节点的文本内容
		 * document.getElementByTagName() 通过标签名获取元素
		 */
	}
}
